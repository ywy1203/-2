# 员工台账管理系统 - 需求说明书 (PRD)

## 1. 产品概述
### 1.1 背景与目标
员工台账管理系统是一个高效、灵活的企业级信息管理平台，旨在帮助人力资源及相关管理部门结构化地记录、追踪和分析所有员工（包括入职、离职、借调等）的各维度数据。过去依靠 Excel 或零散系统的管理方式不仅极易出错，而且难以进行跨维度的数据交叉分析。本系统的核心目标是：
- **数据标准化**：提供统一的录入标准和模板，减少脏数据。
- **管理灵活化**：通过全配置化的底层架构，适应企业不断变化的台账种类需求。
- **决策数据化**：将干瘪的数据表格转化为直观、动态的统计图表，辅助管理层决策。

### 1.2 核心用户画像
- **HR 人事专员/业务执行层**：负责日常台账数据的录入、修改、删除及批量导入，高度依赖“明细视图”与快捷操作。
- **部门主管/管理层**：关注宏观维度的人员流动趋势、结构比例等，高度依赖“多公司维度切换”与“统计分析看板”。

---

## 2. 核心功能与详细交互说明

### 2.1 整体布局框架
采用经典的 `左侧导航菜单 (Sidebar) + 右侧主工作区 (Main Workspace)` 的上下/左右分屏结构，确保用户始终能明确自身所处的位置。左侧菜单可平滑折叠展开以最大化工作区面积。

### 2.2 台账导航与分类体系
- **多层级目录树**：左侧边栏支持多级菜单折叠。包含“调离台账”（下分子节点：入职、离职、借调）、“工作锻炼”、“职业发展”、“领导班子”、“工会活动”、“党建台账”、“其他台账”等分类。
  - **交互反馈**：鼠标悬停（Hover）展现微弱的背景高亮；点击节点后文本及背景变为业务主色调（Blue）并保持 `Active` 状态。
- **快速全局搜索**：边栏顶部自带轻量级搜索框。
  - **输入防抖 (Debounce)**：用户输入关键词时延迟触发搜索，快速过滤并高亮包含该关键词的台账节点名。

### 2.3 多层级数据隔离与管理 (公司级 Tab 区)
- **多公司维度切换**：在右侧工作区顶部，水平排列一组公司实体 Tab（如：交易所、技术公司、数据公司、唐银公司）。
  - **状态保持**：当用户在一级菜单切换不同台账时，当前选中的“公司”状态应当被记忆并保持（如果新台账同样适用该维度）。
  - **视觉隔离**：激活的 Tab 使用沉浸式主题色块（例如主题蓝背景配白字），未激活的 Tab 为浅白底色，边缘使用优雅的分割线进行区隔。

### 2.4 业务全局总览看板 (Global Overview Dashboard)
作为用户登录系统或点击侧边栏“台账总览”后的首屏视图，提供从宏观到微观的数据洞察，帮助管理层迅速掌握系统整体运行状态。
- **关键数据指标 (KPI Cards)**：
  - **系统规模**：展示当前系统纳管的“台账总数”和“台账分类大类”，直观展现业务覆盖面。
  - **数据体量**：展示“累计数据条目”以及“今日发生变更”的数据集份数，体现系统的活跃度。
- **全局数据可视化 (Data Visualizations)**：
  - **各分类台账分布数量**：通过柱状图展示各类（如人事调离、党建议事等）下的台账总数本数，了解哪部分业务管理最重。
  - **台账数据量结构构成**：通过环形图（Donut Chart）拆解展示各主要台账类型的数据条目占比，分析核心业务数据的比重。
  - **各公司台账数据量分布**：采用**堆叠条形图**横向展示不同主体公司在此系统沉淀的数据量级，同时在堆叠颜色中区分各类型台账（如调离台账、锻炼台账等）的构成。
  - **台账变更趋势动态**：通过带有半透明渐变面积的折线/面积图（Area Chart），反映近 30 天内“新增记录”与“修改记录”的双线历史趋势，便于监控业务波动的密集期。
- **快捷直达与操作流入口**：
  - 在页面底部或视口显著区域，提取出高频访问的台账入口卡片。用户可一键点击跳转至相应的具体台账管理界面（即进入明细视图），形成顺畅的“总览 -> 详情” 下钻操作流。

### 2.5 数据明细引擎 (明细视图 - Details View)

#### 2.5.1 复合搜索与过滤区 (Toolbar)
- **关键词综合检索**：
  - 核心包含一个带 `Search` 图标的 Input 输入框。
  - **智能匹配**：无需指定字段，用户输入内容将自动在当前台账的所有可见文本/枚举字段中执行模糊匹配。
- **高级筛选面板 (Filter Panel)**：
  - 点击“高级筛选”按钮平滑展开下拉面板。
  - 面板内根据当前台账类型动态渲染对应的下拉选择器（如：部门筛选、状态筛选等）。
  - **实时联动**：用户在下拉框做出的任何选择将立即作用于下方表格集，无需手动点击搜索。
  - **过滤感知**：当有任意高级筛选条件生效时，筛选按钮旁出现醒目的提示红点或角标，并允许用户一键“清除筛选”。

#### 2.5.2 动态可交互数据表 (Data Table)
- **表头固定与动态列**：表格区域支持区域内独立滚动 (Overflow Scrol)。向下滚动查看海量数据时，表头 (Thead) 始终吸顶 (Sticky) 以防丢失上下文。列的名称、顺序完全继承自统一的 JSON 配置文件。
- **批量操作能力**：
  - 每一行首列包含 Checkbox。表头拥有“全选/反选”总开关。
  - 当检测到有行被选中时，搜索栏右侧（或顶部操作区）**动态滑出/渐显**一条悬浮操作带，显示“已选择 N 项”，并提供“批量编辑”、“批量删除”的主操作入口。
- **空状态指引 (Empty State)**：当无任何数据或搜索结果为空时，表格主体处展示友好的占位插图与“暂无数据”文字，避免用户误以为页面正在加载。
- **右侧固定操作列**：包含每一行独立的“查看/编辑/删除”图标按钮，采用半透明 Hover 才显现的弱设计，减少视觉噪音。

#### 2.5.3 独立的增删改查表单弹窗 (Modals)
- **表单布局**：点击“新增记录”或单行“编辑”后，屏幕覆盖 50% 不透明度的深色遮罩，弹出中央模态框。表单内部使用 `Grid` 布局（双列展现）以压缩纵向空间，无需频繁滚动。
- **必填强校验**：带有红色星号 (*) 的字段采用原生 HTML5 和业务逻辑双重阻断，未填满前“提交保存”按钮将被禁用或点击时即时提示校验错误。
- **连续录入体验**：新增页面底部提供特有的“保存并继续添加”次级按钮。点击后后台保存数据，但弹窗不关闭，且立即清空所有表单域，极大提升 HR 集中录入时的效率。
- **批量编辑逻辑**：点击批量编辑后，打开的表单要求用户：首先在下拉框中选择“要更新的目标字段”，随后输入（或选择）“新的值”。点击确认后，所有勾选行的数据同步更新。

#### 2.5.4 数据流转与落地
- **批量导入 (Import)**：支持拖拽外部 `.csv` / `.xlsx` 文件进入虚线响应区。前端拦截校验文件后缀，支持上传进度动画（概念或模拟）。
- **归档导出 (Export)**：一键将当前表格内的结果集转化为 `.csv` 下载。

---

### 2.6 可视化统计看板体系 (分析视图 - Analysis View)

#### 2.6.1 视图核心协同机制
- **数据一致性同构**：图表所渲染的数据集来源，**必须**严格经过“明细视图”中的全局搜索词以及多重下拉筛选器过滤之后的作用集。从而实现“先查后看”、“边滤边看”的互动体验，业务人员可以随时过滤出“某部门某月份的状态比例”而无需重新配图表。

#### 2.6.2 "编辑态" vs "展示态" (Edit vs Presentation Mode)
为防止误操作和保持看板整洁，每张分析卡片严格区分两种状态：
- **纯粹展示态 (View)**：卡片只展示干净的标题、大图表本身，以及右上角隐蔽的配置齿轮（或直接允许双击进入编辑模式）。鼠标悬停可查看 Tooltip。
- **实时编辑态 (Edit)**：
  - 进入编辑时，卡片采用阴影浮窗或边框变色强调焦点。
  - 图表旁边抽屉式滑出“配置面板 (Configuration Panel)”。
  - 左侧配参数，右侧画布实时即刻响应 (Real-time Preview)，无论改变图表类型还是变更坐标轴字段，图表的形变应当伴有丝滑的补间动画（如利用 Recharts/Echarts 自身的动画属性）。

#### 2.6.3 图表配置化引擎 (维度与度量核心)
图表引擎能够理解数据的元信息 (Metadata)，并自适应展现配置项：
- **(X 轴) 分类维度 (Dimensions)**：
  - **字段适配**：下拉列表会智能筛选当前台账所有文本型 (`text`)、选择型 (`select`/`status`) 和 日期型 (`date`) 字段。
  - **日期下钻交互**：当 X 轴选中诸如 “入职时间” 这样的 `date` 字段时，下拉框下方会通过折叠动画立刻展开一组时间粒度分档栏：【按年】【按月】【按日】。业务点击其中一项，图底部的 X 轴标签自动归一化处理（例如从 `2024-05-12` 自动截取聚合为 `2024-05` 并把所有 5 月份的数据合并计算）。
- **(Y 轴) 数值指标 (Metrics)**：
  - **计算方法 (Aggregations)**：首选支持 3 种聚合逻辑：`记录数 (Count)`，`求和 (Sum)`，`平均值 (Average)`。
  - **数值字段动态过滤**：如果选择了求和或计算平均值，需要进一步指定对哪个字段求和（例如“考勤分数”），此时引擎自动排除所有文本字段，仅提供 `number` 类型字段供选择。

#### 2.6.4 视觉与微交互
- **配色盘序列 (Color Palette)**：多组柱状图或复杂散点将采用一套符合品牌基调的连续或离散色板，避免出现刺眼的纯红纯绿。
- **Empty & Error 态**：当统计出的数值全为 0 导致无法构成如饼图等闭合图形时，要有优雅的容错回退机制（例如在图表中心灰字提示“暂无符合维度的数值”）。

---

## 3. 非功能性需求 (NFR)
### 3.1 性能与响应要求 (Performance)
- **海量数据渲染 (Virtualization)**：后续若遇到单台账超 1000 条明细时，明细表格在未分页情况下需平滑滚动，图表需要在 200ms 内完成清洗和重绘（重绘时防抖动处理）。
- **极速切换体验**：分析视图与明细视图之间的 Tab 切换不得有明显白屏，DOM 重用降低重排（Reflow）消耗。

### 3.2 兼容性与运行环境 (Compatibility)
- **浏览器兼容性**：支持最新版 Chrome, Edge, Safari, Firefox。不再针对 IE11 及以下版本提供重型向后兼容。
- **自适应与响应式 (Responsive Design)**：虽然主应用定位于桌面端后台，但需确保在 1024px ~ 1920px 范围内的任意分辨率下，侧边栏、主工作区、弹窗网格都能自适应伸缩而不打破布局。

### 3.3 架构可扩展性 (Scalability)
- **“零代码”台账创建支持**：虽然当前版本通过硬编码 JSON 配置了各类型台账，但系统架构中的解耦设计必须保证：后续完全可以通过可视化表单来新增不同类型的 `LedgerConfig`，而前端无需修改一行代码即可自动支持新类型台账的 表格展现、条件过滤和图表动态渲染。这是系统生命力的核心。
